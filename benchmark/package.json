{
  "name": "@photostructure/sqlite-benchmark",
  "version": "1.0.0",
  "private": true,
  "// type": "do not mark this as an ESM package -- this library is intended to run in both CJS and ESM modes",
  "description": "Performance benchmarks for @photostructure/sqlite compared to other SQLite libraries",
  "scripts": {
    "prebench": "tsc --noEmit",
    "bench": "tsx index.ts",
    "bench:charts": "tsx index.ts --charts",
    "bench:select": "tsx index.ts select",
    "bench:insert": "tsx index.ts insert",
    "bench:transaction": "tsx index.ts transaction",
    "bench:memory": "tsx --expose-gc memory-benchmark.ts",
    "prebench:async": "npm run typecheck:async && cd .. && npm run build:dist",
    "bench:async": "tsx async-pool.ts",
    "bench:tagstore": "tsx tagstore-benchmark.ts",
    "typecheck:async": "tsc --noEmit --ignoreConfig --target es2022 --module esnext --moduleResolution bundler --esModuleInterop --skipLibCheck --types node --strict async-pool.ts ../src/types/node-gyp-build.d.ts"
  },
  "dependencies": {
    "@photostructure/sqlite": "file:..",
    "better-sqlite3": "13.0.3"
  },
  "devDependencies": {
    "@types/node": "26.1.1",
    "tsx": "4.23.1",
    "typescript": "6.0.3"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
