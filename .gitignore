# Logs
logs
*.log
*debug.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage
*.lcov

# nyc test coverage
.nyc_output

# Compiled binary addons (https://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/
jspm_packages/

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# dotenv environment variables file
.env
.env.test


# Build directories
build/
dist/

# Native addon build files
*.node

# Prebuilds
prebuilds/

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# Test files
test-results/
coverage/
test/*.db
stress-test-results.json
stress-test-results.md

# Generated documentation is now in build/docs/ (covered by build/ ignore)

# third-party libraries for reference (submodules are tracked)
# Note: submodules are tracked in .gitmodules, not ignored

# Temporary directories
.temp-sqlite-download/
packages/better-sqlite3-compat/temp/

# Cache directory (Docker builds, etc)
.cache/

# Sync cache files
.sync-cache.json
.sync-tests-cache.json
.sync-upstream-review.json

# Synced Node.js test files (auto-generated by npm run sync:tests)
# test/upstream/ contains unmodified Node.js originals (reference only)
# test/node-compat/ is committed so CI works without network dependencies
test/upstream/

# Dynamically created by Bear and fed to clang-tidy:
compile_commands*.json

# Release working directories (see RELEASE.md)
package-artifact/
package-install/

.codex
