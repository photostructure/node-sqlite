{
  "#": "used by `npm run watch` and vscode. Note that this doesn't emit anything into ./dist/",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "module": "ESNext",
    "moduleResolution": "node",
    "types": ["node", "jest"]
  },
  "exclude": ["dist", "coverage", "docs", "src/api-compatibility.test.ts"]
}
